# robosuite mainline run instructions

Run every command in PowerShell. The active mainline uses E:\hand_arm_retargeting\.venv-robosuite.

~~~powershell
Set-Location "E:\hand_arm_retargeting"
$PY = ".\.venv-robosuite\Scripts\python.exe"
& $PY --version
~~~

## Current environment, baseline, and validation status

- Platform: Windows; main interpreter: E:\hand_arm_retargeting\.venv-robosuite\Scripts\python.exe
- Verified versions: Python 3.10.20, robosuite 1.5.2, and MuJoCo 3.3.7.
- Main engineering baseline: robosuite + MuJoCo + Panda + OSC_POSE. Panda + Allegro uses a 22-D action vector: [0:6] for the wrist and [6:22] for the 16-joint hand.
- Frozen reference baseline: legacy PyBullet / panda-gym files are hash-audited and are outside mainline changes.
- Verified progress: Panda + Allegro, PandaGripper, Jaco, synchronized three-view rendering, Gate 2 regression, and the isolated official dex-retargeting sidecar.
- Manual validation still required: live SpaceMouse axes and neutral behavior, camera landmarks, right-hand dex motion, button isolation, and safe R/Q/Ctrl+C cleanup during a live session.


## 1. Environment and dependencies

Check the installed mainline packages:

~~~powershell
& $PY -c "import robosuite, mujoco, numpy, cv2, mediapipe, hid; print(robosuite.__version__, mujoco.__version__, numpy.__version__)"
~~~

Synchronize the locked dependencies:

~~~powershell
& $PY -m pip install -r .\requirements-robosuite-wrist.lock
~~~

The main robosuite process does not directly install dex-retargeting. Its native pin>=2.7 dependencies are not reliably co-installable with the current Windows MuJoCo environment. Panda + Allegro launches a separate Python sidecar that loads the checked official dex-retargeting 0.4.6 wheel, receives 21 x 3 MANO landmarks through newline-delimited JSON, and returns 16 named Allegro targets.

With runtime=auto, the application looks for dexretarget\python.exe next to the base Conda environment. If your isolated dex environment is elsewhere, set it before startup:

~~~powershell
$env:DEX_RETARGETING_PYTHON = "D:\your-dex-environment\python.exe"
~~~

Only the sidecar sets KMP_DUPLICATE_LIB_OK=TRUE for the current Conda OpenMP DLL collision. The robosuite / MuJoCo process is not modified.

## 2. SpaceMouse diagnostic

The diagnostic reports device discovery, raw six-axis values, calibrated values, offset, deadzone and smoothing output, final OSC action, button states, frequency, and disconnect errors. Keep the device centered during the initial eight-frame neutral calibration. Missing, stale, or disconnected input produces an exact zero action.

~~~powershell
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py diagnostic
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py diagnostic --duration 30
~~~

In 3DxWare, disable mouse-wheel emulation, system clicks, radial menus, and driver UI actions. Inspect, install, or restore the project profile:

~~~powershell
& $PY .\scripts\manage_3dxware_profile.py inspect
& .\scripts\install_3dxware_spacemouse_profile.ps1
& .\scripts\restore_3dxware_spacemouse_profile.ps1 -Manifest <backup-manifest-path>
~~~

## 3. Interactive entry points

Panda + Allegro defaults to official dex hand control. When two hands are detected, the runtime selects landmarks for the detected right hand instead of assuming hand index zero. runtime=auto accepts only an official native dex runtime or the official sidecar. If neither works, startup fails explicitly and never falls back silently to a rule-based mapping.

~~~powershell
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py integrated
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py integrated --hand-control legacy-curl
& $PY .\scripts\run_spacemouse_panda_jaw_gripper_teleop.py
& $PY .\scripts\run_spacemouse_jaco_three_finger_teleop.py
~~~

The default simulation viewer is a synchronized three-view composite: shoulder-back main view, wrist view, and side view. It renders at native 1920 x 1080: a 1530 x 1080 main image and two 380 x 535 auxiliary images. SpaceMouse motion is transformed from the main camera basis (screen-right, viewing depth, world-up) to world-coordinate OSC_POSE commands. Confirm physical axis directions manually.

Panda + Allegro also opens a live camera status window by default. Use --no-camera-display to hide it or --headless to create no windows. R resets. Q and Ctrl+C quit safely. R and Q work in both the camera and three-view windows; closing either window also triggers safe shutdown.

For Panda + Allegro, the two SpaceMouse buttons toggle Allegro gestures on rising edges. PandaGripper uses HID bit 0 to close and bit 1 to open. Jaco requires the corresponding button to remain held for continuous opening or closing; release stops motion immediately. Confirm physical button mapping with the diagnostic command.

In Panda + Allegro integrated mode only, B enlarges the interactive Lift cube once to 1.5x its default size. R restores the default size. This key does not change OSC, dex, or any other entry point.

The Panda + Allegro laser starts at palm_laser_site and points vertically down in world -Z. It is visual only and never changes physics, collision, or actions.

## 3.1 Shadow Hand audit

Shadow Hand is a strictly isolated fourth entry point. It is not yet a runnable teleoperation program.

~~~powershell
& $PY .\scripts\run_spacemouse_panda_shadow_hand_teleop.py --audit
~~~

The audit writes its report and exits without opening HID, camera, MuJoCo, or a viewer. A regular Shadow Hand startup refuses because robosuite 1.5.2 has no verified native Shadow Hand registration or project-specific mapping. It never runs Allegro instead.

## 4. Headless automated smoke checks

These commands use synthetic zero-motion input. They validate simulation startup, action composition, reset, shutdown, and gripper behavior. They do not validate a physical SpaceMouse, a live camera, or human hand motion.

~~~powershell
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py integrated --headless --headless-smoke --duration 2
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py integrated --headless --dex-smoke --duration 2
& $PY .\scripts\run_spacemouse_panda_jaw_gripper_teleop.py --headless --headless-smoke --duration 2
& $PY .\scripts\run_spacemouse_jaco_three_finger_teleop.py --headless --headless-smoke --duration 2
~~~

--headless-smoke uses a safe-open hand. --dex-smoke keeps the official dex sidecar, camera worker thread, and 22-D Panda + Allegro action layout active without inventing camera landmarks.

## 5. Visible three-view smoke checks

These commands display the composite viewer briefly, save an initial screenshot, and quit automatically. Panda and Jaco smokes synthesize one close and one open command.

~~~powershell
& $PY .\scripts\run_spacemouse_panda_wrist_teleop.py integrated --viewer-smoke --duration 2.6
& $PY .\scripts\run_spacemouse_panda_jaw_gripper_teleop.py --viewer-smoke --duration 2.6
& $PY .\scripts\run_spacemouse_jaco_three_finger_teleop.py --viewer-smoke --duration 2.6
~~~

The interactive Panda + Allegro B key is intentionally outside these synthetic viewer tests.

## 6. Automated regression and Gate 2

~~~powershell
& $PY -m pytest -q .\tests
& $PY .\scripts\run_grasp_validation.py --gate 2
& $PY .\scripts\run_grasp_validation.py --gate 2 --visualize
~~~

The last command only replays the locked Gate 2 static grasp. It does not start Gate 3, workstream 02, motion planning, or a new hand pipeline.

## 7. Frozen-baseline hash check

~~~powershell
& $PY .\scripts\verify_frozen_baseline_hash.py
~~~

This check covers the frozen reference files. If it reports a mismatch, inspect the output manifest first. Never overwrite the frozen baseline from current robosuite work.

## 8. Output, manual checks, and rollback

Panda + Allegro logs are written to outputs\spacemouse_wrist\<timestamp>_integrated. Panda and Jaco logs are written to outputs\gripper_teleop\<timestamp>_*. Each dex run records its runtime, wheel SHA-256, configuration SHA-256, human indices, and 16 joint names in metadata and telemetry. A visible three-view run writes an uncompressed tri_view_laser_initial.png. The outputs directory is local evidence and is excluded from Git.

Before accepting live operation, manually verify the live image and landmarks, right-hand dex motion, SpaceMouse direction and neutral behavior, button isolation, R/Q/Ctrl+C cleanup, and a sustained dex sidecar session.

To disable the laser, set laser.enabled to false in configs\spacemouse_wrist.json. To disable the composite, set tri_view.enabled to false or pass --no-three-view. Set dex_hand.runtime to native to forbid the sidecar and require official dex dependencies inside the active interpreter. To use the old rule-based mapping, explicitly pass --hand-control legacy-curl.

Pre-change copies and their SHA-256 manifest are stored locally under outputs\weekly_grasp_ux\20260916T153432Z\prechange\. Restore those copies to revert this iteration.
